 AI Agent GCS Bucket Setup (12.5 marks)
Creates Bucket
agent-cli
AI Coding Agent
gcloud storage buckets
create gs://q1-...
Google Cloud Platform (GCP)
GCP Project
Your Current
Active Project
Created Bucket Storage
q1-
[q1nonce]
asia-south1 (Public View)
Important Advisory: Practice these steps manually first using the GCP Console GUI and Cloud SDK CLI to understand the flow. Once comfortable, configure your AI coding agent and prompt it to complete all the tasks autonomously in a single session with a single prompt. For grading verification, all task executions must be done solely by your agent.
Prompt your AI coding agent (instead of doing it yourself) to set up Google Cloud CLI access, enable Cloud Storage, and create the assigned Google Cloud Storage bucket in asia-south1.

Download your private task details after logging in. The file contains your assigned bucket name.


Before checking, make the bucket publicly readable/listable so the server can verify it through public GCS metadata. Your agent log should show the Storage API enablement, bucket creation, and bucket describe/list output.

agent_log_jsonl_file content
Paste the complete JSONL log from your AI coding agent session
The Check button verifies the Google Cloud Storage bucket for 6.25 auto-graded marks. The remaining 6.25 marks are graded offline from this pasted agent log after the deadline.
